# Configure these through your host's environment manager; Node does not auto-load this file.
HOST=127.0.0.1
PORT=8787
# Set only when the browser and API use different origins.
APP_ORIGIN=
# Use a private writable path outside the static site root in production.
DUEL_STATE_FILE=
FEEDBACK_FILE=
